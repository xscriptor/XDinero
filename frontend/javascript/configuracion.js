/**
 * CONFIGURACION.JS — XDinero
 * Variables globales de configuración
 */

var XDConfig = {
    // URL base de la API PHP (relativa, a través del proxy Nginx)
    urlApi: '/backend/api',

    // URL base para recursos estáticos (imágenes de uploads, avatares)
    urlBase: '/backend',

    // URL base del frontend (para compartir enlaces)
    urlFrontend: window.location.origin
};
